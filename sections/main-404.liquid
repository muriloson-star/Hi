<section class="section">
  <div class="page-width text-center" style="padding-block:4rem">
    <span class="eyebrow eyebrow--leaf">404</span>
    <h1>{{ '404.title' | t }}</h1>
    <p class="section-subtext" style="margin-inline:auto">{{ '404.subtext' | t }}</p>
    <a href="{{ routes.root_url }}" class="button--primary button--lg" style="margin-top:1.8rem">{{ '404.link' | t }}</a>
  </div>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
